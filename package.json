{
  "name": "toolhouse-agenticlabs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute agenticlabs-db --local --file=schema.sql",
    "db:migrate:remote": "wrangler d1 execute agenticlabs-db --remote --file=schema.sql"
  },
  "dependencies": {
    "hono": "*",
    "openai": "*",
    "zod": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "*",
    "typescript": "*",
    "wrangler": "*"
  }
}
